export const CONFIG_KEY = "setup";

/** Der Hub selbst. Die Module bringen ihre eigenen Präfixe mit. */
export const PANEL_PREFIX = "setup:hub";

/** Button, mit dem jedes Modul zurück in die Übersicht führt. */
export const BACK_TO_SETUP = `${PANEL_PREFIX}:home`;
